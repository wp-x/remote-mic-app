# 版本历史

## 1.9.21（预发布）

- 提高版本更新检查的可用性，减少偶发无法获取更新信息的问题。

## 1.9.20（预发布）

- 新手引导统一使用 Fn 语音键，减少首次配置时的模式混淆。
- 对已有语音键设置提供迁移提示，帮助完成新版配置。
- 加强首次使用的麦克风、音频输出与语音结果诊断，失败原因更清晰。
- 修复遥控器仍在连接准备时，Fn 点击模式可能被意外关闭的问题。
- 更新项目说明中的展示图片。

## 1.9.19（预发布）

- 改进 Onboarding 权限页返回、语音测试输入框和诊断反馈。
- 未安装已知语音工具时保留“其他语音工具”入口，并提供豆包官方安装入口。
- 改进“回眸”录音播放失败提示，帮助区分记录缺失、文件修改、读取失败和播放器启动失败。
- 修复遥控器唤醒后语音键映射未恢复的问题。
- 卸载 SayAll 时将相关 App、历史路径和驱动移入废纸篓，安装替换失败时支持回滚。
- 移除“回眸”仅录音记录上的 Finder 入口，保留播放、导出和移入废纸篓。

## 1.9.18（预发布）

- 修复大量回眸记录浏览时的重复、空白和滚动卡顿。
- 全部应用默认显示最近 7 天记录，选择具体应用可查看完整历史。

## 1.9.17（预发布）

- 改进首次设置中的语音工具配置，支持微信输入法、Typeless 和豆包的安装状态识别与引导。
- 首次设置新增 Fn/地球键、左 Command、右 Command 语音触发模式选择；Typeless 保持 Fn 点按行为。
- 选择语音输入法后自动切换对应输入源，并减少升级用户重复进入完整首次设置。
- 修复预览版 Build 高于候选包时的语义版本更新误判。
- 移除会打开文本下载链接的版本历史按钮，关于页直接显示最新版本的本地化更新内容。

## 1.9.16（预发布）

- 新增左 Command 和右 Command 语音触发模式，默认 Fn/地球键行为保持不变。
- 语音键按下后立即开始、释放后立即结束；输入框聚焦改为普通遥控器按键的独立动作。
- 改进 Electron、Web、搜索框和 Terminal 等前台输入框的聚焦可靠性。
- 修复 RC003 TV 键在不同键盘布局下向前台文本框泄漏字符的问题。
- 改善 Mac 从睡眠唤醒后的蓝牙恢复和音频故障诊断。
- 支持使用遥控器控制 macOS Command-Tab 应用切换器。
- 为不可连发动作增加可选的快速连续按功能，默认保持关闭。
- 修复组合动作编辑页面误消费普通遥控器按键的问题。
- 新增本地保存原始录音，并改进没有可编辑输入框时的回眸记录。
- 新增向上滚动和向下滚动遥控器动作。
- 调整“关于”页语言选择器的布局对齐。

## 1.9.10（预发布）

- 组合动作现在默认开放，无需邀请码即可查看、创建、绑定和使用。

## 1.9.9（预发布）

- 在“关于 → 应用偏好”中新增可选的“登录时自动启动”，需要 macOS 批准时可直接打开“登录项”设置。
- 新增页面内键盘快捷键选择，可选择常用键、方向与导航键、F1–F20、数字键盘以及独立的左右修饰键，同时保留实体键盘录入方式。
- 改进首次设置和权限恢复：窗口会在按内容尺寸调整后居中，Input Monitoring 或 Accessibility 权限变化后已启用的自定义按键映射会自动恢复。
- 语音输入现在只选择已启用的首选输入法，避免重复触发确认提示；除非你在会话中手动改选，否则会话结束后恢复原输入法。
- 改进失效缓存遥控器的蓝牙恢复，采用有界退避并处理蓝牙电源恢复，同时保留已保存的配置和映射。
- 减少语音流期间无效的界面刷新，同时保持音频处理和诊断行为不变。
- 防止同时启动多个无线麦实例；再次打开 App 时会激活已有实例，不会重复初始化蓝牙、音频或 HID。
- 在不同 App 之间切换时保持设置窗口可见，并在窗口真正关闭前保留 Dock 图标。
- 修复 RC003 monitored 模式下将电视键绑定为自定义动作时向前台文本框泄漏“§”字符的问题。

## 1.9.8（预发布）

- 改进从旧版 Remote Mic 安装升级的可靠性：更新音频驱动前会先停止旧版 App，新版无线麦SayAll.app 保持在标准 Applications 路径，升级后可以更稳定地继续音频设置。

## 1.9.7（预发布）

- 修复首次语音设置过程中 macOS 输入法确认提示反复出现的问题；只有在语音工具步骤需要时才会切换选定的输入法。

## 1.9.6（预发布）

- 首次设置中的权限卡在已授权后仍可点击，方便重新打开对应的 macOS 隐私设置检查或修复权限。
- 已安装用户升级后如果缺少必要权限，会直接进入权限修复页面。
- 改进首次设置中的实体遥控器发现：首次真实按键即可选择正确设备，不再因等待首个 HID 报告而长时间卡住；当其他输入工具独占设备时提供更明确的处理提示。

## 1.9.5（预发布）

- 修复未绑定的实体遥控器在首次设置中已被发现、但首次按键可能无响应的问题；首次设置现在可以可靠接受第一次输入。

## 1.9.4（预发布）

- 在 Mac 开启“连接手机”后，iPhone 可扫描短时二维码直接连接当前局域网；直连失败时会自动回退原有的 Bonjour/P2P 自动发现，旧版 iPhone 与 Apple Watch 的连接方式保持不变。

## 1.9.3（预发布）

- 首次设置会正确同步 Typeless 的 Fn 点按设置；实体遥控器的普通按键已被识别时，也能继续后续连接检查。
- 组合动作不再需要邀请码；在包含对应私有模块的构建中可以直接使用，其他受邀功能的独立访问限制保持不变。

## 1.9.1（预发布）

- 应用统一更名为无线麦SayAll.app，并支持从旧版 Remote Mic.app 和无线麦.app 安装迁移。
- 组合动作可将打开 APP、等待、快捷键和输入框聚焦组合后绑定到遥控器按键。
- 新增仅保存在本机的“回眸”语音记录，支持撤销删除；SayAll MCP 默认关闭，启用后仅提供只读访问。
- 重新整理首次设置流程，明确支持 MiRemoteV 2ch、BlackHole 2ch 等兼容设备，并在完成前验证真实语音输入。
- 改进 Apple Watch 直接语音输入与停止后快速重启处理；完整真机场景仍在持续验证。
- 修复首次聚焦输入框后，第一句语音可能未记入“回眸”的问题。
- 改进虚拟音频在输出状态过期、Mac 休眠或唤醒后的释放与恢复。
- 在侧边栏、关于页和统计页增加分享入口，并将问题反馈集中到关于页。
- 权限缺失时提供更明确的升级检查与恢复提示。
- 增强 HID 按键链路的分层诊断信息，用于继续定位偶发丢键。

## 1.8.25（预发布）

- 在打开错误架构的安装包时，会明确提示当前 Mac 所需的正确版本。
- 更新 SayAll 品牌信息，并将应用内官网入口统一为 sayall.app。
- 改进首次设置中的遥控器配对指导，补充唤醒遥控器以及同时按住 Home 和菜单键进入配对的步骤。
- 改进 iPhone、Apple Watch 和网页语音连接：不同来源彼此隔离，并显示真实连接状态和取消连接入口。
- 菜单栏新增问题反馈入口，方便直接提交使用中遇到的问题。

## 1.8.23（预发布）

- 修复从侧边栏打开“快捷指令”时 App 可能意外退出的问题。
- 改进安装包内“快捷指令”页面资源的加载方式，使全新安装和升级后都能稳定打开。

## 1.8.22（预发布）

- 修复部分设置页面在中文环境中显示原始文本标识的问题，并收紧输入与状态区域的布局。
- 设置窗口中的文本框现在支持标准 Mac 编辑快捷键，包括复制、粘贴、剪切、撤销、重做和全选。
- 更新 Apple Watch 直连共享组件，改进蓝牙回退与通知重试；发现、授权、按键和麦克风流程仍待真机验收。

## 1.8.21（预发布）

- 预发布更新不再在后台自动弹出更新提示；需要进入“关于”页并主动检查后才会显示预览候选。
- 正式更新通道继续保留自动检查和更新提示。

## 1.8.20（预发布）

- 改进“关于”页中的部分预览功能入口体验；功能开关仍保持默认关闭，需要用户主动开启。
- 改进预览功能设置时的状态反馈和恢复体验。

## 1.8.19（预发布）

- “连接与语音”页新增 Apple Watch 专用连接入口，可按需开启或取消附近设备等待；Apple Watch 的发现、授权、按键和麦克风流程仍待真机验收。
- 新增快捷指令预览功能，可组合打开 APP、等待 APP、快捷键和输入框聚焦等操作，并绑定到遥控器按键。
- 改进部分预览功能的状态恢复和错误提示，并修复最终安装包首次打开设置窗口时可能意外退出的问题。

## 1.8.12（预发布）

- 优化 Mac 安装包和自动更新文件的下载速度与稳定性；现有版本仍可正常检查更新，并继续保留 GitHub 下载作为备用。

## 1.8.11（预发布）

- 修复新配对的遥控器已经在 macOS 中连接，但首次设置向导仍持续查找、必须重启无线麦才能显示的问题；从系统蓝牙设置返回后会自动重新发现并连接设备。
- 修复遥控器显示已连接但普通按键没有反应、只有 macOS 音量等系统操作生效的问题；多个底层按键服务只有部分可用时，安全的遥控器仍可继续工作。
- 首次设置向导现在会显示真实的按键监听状态并提供重新检测入口，音频设备也会在返回无线麦后自动刷新；进入主面板前会再次确认权限、遥控器和音频输出仍然可用。
- 遥控器卡片完整显示“小米蓝牙遥控器 2”和“小米蓝牙遥控器 2 Pro”，使用电量与充电标识区分电池状态；文件菜单新增“打开日志所在文件夹”。

## 1.8.10（预发布）

- 已经安装并使用过无线麦的用户升级后不再重复进入首次设置向导；真正全新安装、尚未完成向导和主动重新运行的用户仍会继续完成必要检查。
- 首次设置向导不再强制使用 MiRemoteV 2ch；现在可直接选择 BlackHole 2ch 或其他已安装的音频设备，并在实际输出就绪后继续。
- 修复从旧版本升级后，设置向导已经收到实体遥控器按键但仍持续显示“正在查找遥控器”的问题；现在会自动恢复蓝牙连接。
- 修复遥控器蓝牙断开后兼容麦克风仍保持活动、可能继续占用会议或语音输入应用的问题；断开后会结束当前语音并释放虚拟音频设备。
- 移除未实际生效的普通遥控器长录音续期尝试，避免额外的强制停止和重连；约一分钟录音限制仍在继续研究。

## 1.8.9（预发布）

- 修复通过遥控器按键打开或切换到目标 APP 后，第一次模拟 Fn 语音有时无效、需要重复尝试的问题；现在会等待目标输入位置真正可用，并保留等待期间说话的开头。
- 如果目标 APP 被关闭、切换、等待超时或焦点落在敏感字段，本次语音会安全取消，不会发送到错误窗口。

## 1.8.8（预发布）

- 新增完整的首次设置向导，逐步检查蓝牙、输入监控和辅助功能权限，以及遥控器连接、兼容麦克风、真实语音输入和常用按键；必要条件未通过时不能跳过或进入主面板。
- 已安装用户可在“关于”页重新运行设置向导，用于重新检查环境和排查问题；现有连接、按键映射、兼容麦克风及其他设置不会被清除。
- 设置向导的浅色与深色模式使用一致的双栏界面，不显示步骤总数；未完成时重新打开 APP 仍会进入向导，不能绕过。

## 1.8.7（预发布）

- 重新设计“关于”页，将当前版本、可用更新、检查更新、版本历史和预发布更新通道集中显示。
- 检测到新版本时，“关于”页会在更新前直接展示当前语言的版本更新内容。
- 切换正式版或预发布更新后会立即刷新结果；预发布源不可用时仍会静默回退正式更新。

## 1.8.6（预发布）

- 修复在其他 APP 输入框中按住遥控器左右键时，文本光标无法持续移动的问题；普通左右方向映射现在使用 macOS 原生按住连发。
- 快捷键只在点击“录入快捷键”后开始捕获，并可录入 Command-Space 等已被系统或其他 APP 占用的组合键；完成后会显示成功提示。
- 修复拖动增益滑块或设置页内容时带动整个窗口的问题；窗口仅保留顶部专用区域可拖动。

## 1.8.5（预发布）

- 长按实体遥控器语音键时会定期续期语音会话，尝试突破约一分钟的录音限制，先期最长三分钟。
- iPhone 无线麦支持点按开始录音、再次点按停止，同时保留按住说话、松手停止。

## 1.8.4（预发布）

- “基础按键”新增 Command-W、Command-X、Command-A、Command-Z、Command-Shift-Z、Command-F 和 Command-S，常用关闭窗口、剪切、全选、撤销、重做、查找及保存操作可以直接映射到遥控器按键。
- 无线麦现在支持标准 Command-Q 退出和 Command-W 关闭设置窗口；关闭窗口后菜单栏功能继续运行。

## 1.8.3（预发布）

- 修复通过 Sparkle 升级后，“启用自定义按键”仍显示开启但实体按键没有立即生效、必须手动关闭再开启的问题；升级完成后会自动等待旧进程释放 HID，再重建一次按键监听。

## 1.8.2（预发布）

- 按键现在可以打开任意本机 APP，并通过 APP 自身快捷键或已学习的辅助功能输入框自动聚焦；切换动作或暂时禁用后，已经录入的快捷键、APP 和输入框设置仍会保留，并支持配置导入导出。
- 重新整理按键动作页面：常用动作按类别直接展示，具体 APP 可按需展开，快捷键录入和输入框学习都在当前页面完成；同时放大中文文字，并将“禁用按键”改为更醒目的独立开关。
- 新增 Command-Return、Shift-Return、Command-C、Command-V、Command-Q，以及 Command-左箭头和 Command-右箭头等固定组合键。
- 修复同时连接多只尚未完成按键绑定的遥控器时，系统枚举顺序可能导致其中一只遥控器的单击和双击无响应的问题。

## 1.8.1（预发布）

- 修复小米遥控器 2 的短语音在松开语音键后可能整段消失或丢失尾音的问题；语音结束后会等待已经排队的声音播放完成。
- 加强小米遥控器 2 与小米遥控器 2 Pro 同时连接时的语音稳定性，普通虚拟麦克风和“模拟 Fn 点按”两种方式均可正常交替使用。

## 1.8.0（预发布）

- 支持同时连接多只小米遥控器 2 和小米遥控器 2 Pro；每只遥控器保留独立快捷键，新遥控器会复制当前设置，操作实体遥控器时页面自动切换到对应设备。
- 连接页显示遥控器型号、电量及可读取的充电或供电状态；按键映射页改为遥控器居中，并在一屏内展示全部按键的单击、双击和长按设置。
- 连线从实体按键热点指向对应设置，按下按键时显示位置标识；优化曲线路径、箭头间距、标题开关和功能说明，使映射关系更直观。
- 修复多遥控器按键串台、页面选择跳动，以及长按自定义快捷键、方向键或删除键时连续执行和播放系统错误音的问题。
- 启用自定义按键但缺少权限时，会明确提示并引导到权限页面；授权返回后自动应用设置。

## 1.7.8（预发布）

- 重新设计“连接与语音”页面，按实体遥控器、语音输出、兼容麦克风和手机连接重新组织信息，保留重连、设备选择、TestFlight、网页版和受信任设备等原有功能。
- 重新设计“按键映射”页面，一眼展示全部按键的单击、双击和长按设置，并可锁定当前编辑按键，避免操作实体遥控器时误切换。
- 将“语音键模拟 Fn 点按”移到遥控器图片下方，让语音键的固定行为与相关设置集中显示；配置和实际功能保持不变。

## 1.7.7（预发布）

- 修复 `1.7.6` 在部分连接了小米蓝牙语音遥控器的 macOS 26 设备上启动即退出的问题；HID 映射读取和写入期间会持续保留所属系统客户端。
- 手机网页版和附近 iPhone/iPad 的可配置按键现在支持与实体遥控器一致的单击、双击和长按动作，并分别维护手势状态，避免同时连接时互相影响。
- 增加最终 ZIP 与 PKG 打包产物的启动硬门禁：发布前必须在连接遥控器的交互式 Mac 上完成两次启动、正常退出和崩溃报告检查。

## 1.7.5

- 检查预发布版本时，如果候选版本不存在、网络超时或候选源暂不可用，会静默回退正式更新源，不再显示错误弹窗。
- 修正日、周统计的历史归属：缺少日期的数据只保留在“全部”，不再推测为更早产生的数据。
- 为后续统计展示增加更多仅保存在本机的聚合元数据，包括输入来源、按键或语音入口、小时分布、语音会话次数与时长、最长会话及首末活动时间；不会保存设备标识、用户文本或音频。

## 1.7.4

- 优化统计页本机语音排行的说明，让数据保存范围和展示内容更直观。

## 1.7.3

- 修复更新通道回退：开启预发布版本检查后，即使候选更新源不可用或已经过期，也不会再阻止正式版更新被检测到。
- 新增仅保存在本机的单次语音时长 Top 10，从本版本开始记录，不上传任何使用数据。

## 1.7.2

- 连接页与按键映射页统一更换为更清晰的遥控器产品图，并保持实物按键热点与映射操作准确对齐。

## 1.7.1

- 修复周统计与全部累计不一致：周图现在显示最近 7 周和“更早”汇总，完整覆盖旧版累计及七周以前的数据，同时不伪造历史日期；语音柱状标签改为精确的分秒格式，柱状标签合计也与全部一致。

## 1.7.0

- 修复正式版遗漏手机网页版服务器地址的问题；发布构建和最终 App 校验会在地址缺失时直接失败。
- 邀请码弹窗明确说明 iOS TestFlight 公测无需邀请码，并修复 Return 可能重复提交以及二维码页切换不稳定的问题。
- 移除统计周期切换时的多余动画，并精简“关于”页中的本地数据说明。

## 1.6.11（预发布）

- 新增独立统计页：支持最近 7 天与最近 8 周柱状图，并保留简洁的全部累计数据视图。
- 重新整理窗口和关于页样式，放大默认窗口，隐藏滚动条与页面顶部重复副标题，并突出官网和 GitHub 入口。
- 手机网页版连接增加会话级邀请码；邀请码弹窗优先推荐 iOS App，并提供 TestFlight 公测页面与复制链接操作。

## 1.6.9（预发布）

- 网页扫码后必须手动点击“连接 Mac”才会建立会话，麦克风仍只在按住说话时请求权限。
- 支持已批准网页会话在短时断线后自动恢复，不再因一次网络波动立即要求重新扫码。
- 改进手机语音的自动增益、降噪、弱网缓冲和尾音收尾，避免松手时丢弃尚未播放的语音。

## 1.6.8（预发布）

- 新增无需安装的手机网页版遥控器，使用一次性二维码并由 Mac 每次明确允许连接。
- 新增白名单遥控按键、自定义标题同步和按住说话，并接入现有虚拟麦克风链路。
- 新增短时中继会话、传输加密和限流保护；中继服务不保存语音内容。

## 1.6.7（预发布）

- 修复 iPhone 长时间断连后，新连接可能被 Mac 中残留的旧会话阻塞、只能重启 Mac App 才能恢复的问题。
- iPhone 遥控页面改用浅银色铝合金拉丝背景，移除顶部 Logo 并居中显示连接状态；全部可配置按键均可显示简短自定义文案。
- 增强 iPhone 按住说话的按下动效和双阶段震动反馈，并预热复用录音管线以缩短首句等待。

## 1.6.6（预发布）

- “关于”页新增默认关闭的“检查预发布版本”开关；开启后，Sparkle 的自动与手动检查都会包含最新 GitHub pre-release 候选版本。
- 预发布更新源会在手动检查前及 App 常驻运行期间刷新；无法取得候选信息时不会影响原有稳定版更新源。

## 1.6.5（预发布）

- 修复 iPhone 按住说话的按下与松开反馈，并让手机语音沿用既有 Fn/地球键触发和虚拟麦克风输出链路。
- iPhone 中间六键固定为“返回 / TV / 音量+、主页 / 菜单 / 音量-”。
- Mac 存在非默认按键配置时，iPhone 对应按钮会同步显示不越界的简短动作标题。

## 1.6.4

- 手机遥控改为按需启用的体验与备用功能；Mac 启动时不再自动开启附近连接，只有用户点击“连接手机”后才开始等待 iPhone。

## 1.6.3

- 将 iPhone 与 Mac 首次连接时的校验码由六位调整为两位，并保持双方同步显示。

## 1.6.2

- 首次允许手机连接后会记住该次 App 安装，后续附近连接无需重复授权，并可在设置中清除受信任手机。
- 优化双方校验码的同步与醒目显示，修复重启 iOS App 后授权弹窗可能不同步的问题。
- 修复 iPhone 麦克风权限已开启时仍可能无法开始录音的问题，并继续向普通用户隐藏底层技术错误。

## 1.6.1

- 新增 iPhone/iPad 附近遥控与按住说话连接，使用配对校验码和加密传输。
- 手机按键复用 Mac 当前按键映射，手机麦克风接入现有虚拟音频输出。

## 1.6.0（预发布）

- 将全部界面文案迁移为稳定的语义本地化 key，并完善英文回退与动态语言资源验证。
- 普通界面改用面向用户的产品语言，并在“关于”页新增中英文术语表入口。
- 隐藏可见窗口标题和分隔线，让页面背景与 macOS 原生窗口按钮区域自然融合。

## 1.5.1

- 普通启动默认打开主面板，并可在“关于”页面关闭此行为。
- 更新安装完成并重启后，无条件将主面板与更新完成提示置于最前方。

## 1.5.0

- 重新设计“关于”页面，将版本号与检查更新放在同一区域。
- 语言选项改为始终完整展示，并新增应用内版本历史。
- 同步修正 macOS 14 支持、安装、发布与技术文档。

## 1.4.13

- 修复语言切换重建菜单时重复挂载菜单项导致的 AppKit 异常。

## 1.4.12（预发布）

- 将最低支持版本降低到 macOS 14.0，继续仅支持 Apple Silicon。
- macOS 26 保留 Liquid Glass，macOS 14/15 使用兼容样式。

## 1.4.11

- 新增配置导入与导出。
- 新增仅保存在本机的按键次数和语音时长统计。

## 1.4.10

- 修复 cmux 已位于前台时无法重新聚焦终端输入区域的问题。

## 1.4.9（预发布）

- 改进 cmux 终端重新聚焦和发布说明生成。

## 1.4.8（预发布）

- 改进 Codex、Claude 和 cmux 的输入区域自动聚焦。

## 1.4.7（预发布）

- 新增受保护的 pre-release 发布与正式版晋升流程。
- 新增打开受支持应用后自动聚焦输入区域。

## 1.4.6

- 修复普通用户安装时被要求下载 Xcode 命令行开发者工具的问题。

## 1.4.5

- 修复 Mac 睡眠唤醒或音频路由变化后打开页面可能崩溃的问题。
