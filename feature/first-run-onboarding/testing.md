# 功能测试说明

测试或修改 Onboarding 前，必须先阅读跨平台 [`PRODUCT_SPEC.md`](PRODUCT_SPEC.md) 和当前平台附件。本文件只记录测试入口与历史结果，不替代产品行为规范。

本功能的完整人工测试步骤统一维护在 [`Testing/FirstRunOnboarding.md`](../../Testing/FirstRunOnboarding.md)。

`v1.8.8 (100)` 公开 Pre-release 已通过原流程验证；2026-08-17 两层控制方式候选已通过 233 项 Swift 测试，并逐张检查实体遥控器浅/深色各 9 张、iPhone 和网页版浅/深色各 10 张 Onboarding 生产视图截图。真实 TV 键激活、Home + Menu 配对、RC001 / RC003、新 iPhone Nearby 会话和手机 Safari 网页版仍需真机验收。

2026-08-18 语音测试输入框候选修复已通过 22 项 Onboarding 定向测试、234 项完整 Swift 测试与 42 项项目自检，并检查实体遥控器路径浅色/深色生产语音测试页。首次进入、回到前台和点击重试后的真实豆包/微信输入法文字提交仍需用户现场验收。

2026-08-18 手机按需音频门禁候选验证了“所选设备存在但播放器未持续运行”即可进入语音测试和完成页，并统一要求三种控制方式开启蓝牙、输入监控和辅助功能。2026-08-19 产品规则明确为允许 MiRemoteV 2ch 与 BlackHole 2ch，拒绝普通系统输出。本地包还必须包含 Web Relay 配置；真实 Nearby/WSS、二维码扫码、手机语音开始/停止和文字上屏仍需用户现场验收。

2026-08-19 音频与文字错误通过候选允许 MiRemoteV 2ch 与 BlackHole 2ch，拒绝 Beosound、扬声器等普通输出；完成语音会话后再用键盘输入仍不能点亮文字检查。真实虚拟音频输出和第三方输入法写入来源仍需现场验收。

2026-08-19 Typeless 与实体遥控器识别候选新增两项回归：四种语音工具选择会同步 Fn 点按偏好，三种控制方式通过权限页都会应用；生产 HID 普通按键可以在语音 bridge 尚未 Ready 时证明实体控制已识别，同时后续真实语音门禁保持不变。26 项 Onboarding、312 项完整 Swift 测试、42 项项目自检、Apple Silicon / Intel Release 构建与实体路径 18 张生产页面检查通过；真实 Typeless 与 RC001 / RC003 仍需现场验收。

1.9.3 真实日志确认未绑定 HID discovery 首报告死锁：设备匹配 157 次但没有一次 `HID REPORT` 或 `HID CONNECTED`。候选实现改为非独占探测安全候选，只有包含已知普通按键的首份真实报告才绑定实际设备，空闲或未知报告不会抢占；Remote buttons 86 项、Onboarding 26 项、完整 Swift 314 项/31 suites、硬件事件回放 21 项、项目自检 42/42 与 Apple Silicon / Intel Release 构建通过。真实 IOHID 回调、RC001 / RC003、双遥控器及电源键保护仍需按用例 4B-1 现场验收。
2026-08-20 权限卡候选增加已授权状态回归：蓝牙、输入监控和辅助功能整卡仍可点击并打开各自系统隐私页，已授权蓝牙不会额外重连。Onboarding 27 项、完整 Swift 313 项/31 suites、项目自检 42/42 通过；权限页浅色/深色生产视图无裁切、内部滚动或黑白分栏。正式签名改名权限连续性必须使用同 Team、Bundle ID 和 designated requirement 的 Remote Mic.app → SayAll.app 升级验证，ad-hoc 包不计入通过。

2026-08-21 修复输入法确认重复弹出：豆包/微信只在输入法选择页主动调用系统输入源切换；进入后续页面、输入框重新聚焦或 App 回到前台不再重复调用，完成后的 Fn 自动切换监视器保持不变。自动化与 Release 构建需通过；真实系统确认框、豆包/微信文字上屏仍需现场验收。

Issue #100 增加升级首次启动矩阵：已完成用户在更新后缺任一权限时直接进入“权限与隐私”；全部正常、非更新启动和未完成 Onboarding 均不触发。Onboarding 28 项、完整 Swift 314 项/31 suites、项目自检 42/42 通过，权限页浅色/深色 `800 × 650` 无裁切。真实安装失败必须提供 `/var/log/install.log`，不能用删除旧 App 后偶然成功替代根因验证。

1.9.3 真实日志确认未绑定 HID discovery 首报告死锁：设备匹配 157 次但没有一次 `HID REPORT` 或 `HID CONNECTED`。候选实现改为非独占探测安全候选，只有包含已知普通按键的首份真实报告才绑定实际设备，空闲或未知报告不会抢占；随后补充 `kIOReturnExclusiveAccess` 的通用占用提示和 Karabiner-Elements 专项操作说明。真实 IOHID 回调、RC001 / RC003、双遥控器、电源键及第三方 HID 工具仍需按用例 4B-1、4B-2 现场验收。

2026-08-18 `codex/v1-9-0-onboarding` 固定基线集成复验：23 项 Onboarding 测试、237 项完整 Swift 测试、42 项项目自检、arm64 与 x86_64 Release 构建全部通过；三种控制方式共 58 张 Onboarding 截图逐张检查通过。另检查 800×650 设置页时发现固定主线基线的“按键映射 / Buttons”页标题与启用开关文案被挤压成竖排，中英文、浅深色均复现；该问题不是 Onboarding 七提交引入，但在 1.9.0 发布候选宣称全部设置入口通过前必须单独修复并复验。

交付前至少确认：

- 首次启动无法进入主面板，也没有跳过或稍后继续入口；
- 从 Onboarding 加入前的已安装版本升级后直接进入主面板并保留原设置；全新安装、中途续接和主动重新运行仍显示向导；
- 退出并重启恢复到保存页；
- 第一层并列提供“有遥控器 / 当前没有遥控器”并优先实体遥控器；只有选择没有遥控器，下一页才提供推荐 iPhone App 和“没有 iPhone 时”使用网页版；未选择时不能继续；
- 生产状态已确认遥控器连接时自动进入实体流程，不显示有无遥控器问题，也不为判断提前触发蓝牙权限；
- 实体遥控器、iPhone App 和网页版都要求蓝牙、输入监控和辅助功能全部开启；
- 三项权限已显示授权后，逐张点击仍能打开正确的系统隐私页；点击已授权蓝牙不重建当前连接，也不改变权限门禁；
- 已完成用户刚更新且任一权限缺失时直接进入“权限与隐私”；权限全部正常、普通启动和新用户 Onboarding 不误触发；
- 每项实时条件缺失时“继续”不可用；
- 音频页只展示并接受 MiRemoteV 2ch 与 BlackHole 2ch；Beosound、扬声器和其他普通设备不能满足门禁；实体遥控器要求所选虚拟设备持续输出就绪，iPhone/网页在下一页通过真实按需语音验证；
- 所选控制方式的真实语音必须同时出现同一来源的会话开始、PCM 样本、松开停止和文字上屏；
- 进入语音测试页、从其他 App 返回和点击重新测试后，不手动点击输入框也能直接写入；占位符、光标和正文起点一致；
- 完成一次无文字的语音会话后，手动键盘输入或粘贴不能通过；下一次真实语音开始会清空旧手动文字；
- 所选控制方式的三个不同普通按键被真实检测后才能完成，其他同时在线来源不得代替通过；
- 已安装用户通过 Sparkle 升级后首次启动，即使 HID 按键先到而 BLE 状态仍在查找，也能在不重启 App 的情况下只恢复一次连接；
- 从系统蓝牙设置返回后无需重启即可发现新配对遥控器；多个 HID service 部分映射失败时，安全设备普通按键仍可用且电源键不触发锁屏；
- 未绑定遥控器匹配后日志出现 `HID DEVICE probing mode=monitored`，首个普通按键产生 `HID CONNECTED mode=probed` 与真实 report/edge；双遥控器仍按首份报告分别绑定且不重复执行；
- HID 打开返回 `kIOReturnExclusiveAccess` 时显示通用第三方输入工具占用提示；Karabiner-Elements 已安装时显示关闭 “Modify events” 或设为 “Ignore” 的具体步骤，关闭后无需重启即可重新检测；
- 遥控器页完整显示首次连接顺序：TV 键长按约 2 秒至底部白灯闪烁，再按 Home + Menu 进入配对；中英文均不得裁切；
- 豆包/微信输入法选择后自动切换到对应系统输入源；两条分支显示各自浅色/深色设置截图，并共同显示系统 Fn 释放检查和微信 App 快捷键截图；
- 语音工具四个选择卡等高、顶部对齐；豆包/微信四项设置位于右栏，切换任一指引时页面不滚动、不裁切；
- 系统“按下 Fn/地球键时”未设为“不执行任何操作”时不能从输入法选择页继续；截图模式不得改变用户当前输入法；
- 系统 Fn 第 3 项在浅色/深色下显示对应 macOS 键盘设置截图；完成后豆包/微信分支按 Fn 可自动准备已保存的输入法，Typeless/其他工具不切换；
- Typeless 在实体遥控器、iPhone 和网页版三条路径通过权限页后都开启语音键模拟 Fn 点按；切换到豆包、微信或其他工具后关闭；
- 系统蓝牙已连接但语音 bridge 尚未 Ready 时，生产 HID 普通按键能够显示“已识别实体遥控器”并继续后台恢复；普通键盘不能代替，后续语音流仍必须真实通过；
- 遥控器页从系统设置返回时同时刷新 BLE 和 HID，按键失败原因可见且“重新检测按键”可以恢复；音频页从外部设置返回时自动刷新设备；
- 完成页在当前分支权限、所选控制连接或音频失效时不能进入主面板，恢复实时条件后可继续；完成页重建不会因前序临时标志清零而永久卡死；
- 完成后进入现有设置页，“关于”页能够重新运行向导，并且语音输出设备、按键映射、Dock、启动和更新设置不被清除；
- 实体遥控器 9 页、iPhone/网页各 10 页在 `1020 × 772` 下中英文没有内部滚动或裁切，中文文字不小于 12pt。
- 正式签名旧版 Remote Mic.app 升级为相同代码身份的 SayAll.app 后，三项权限与真实能力延续；本地 ad-hoc 或第三方重签名包不得作为该项验收证据。
- “文件 → 打开日志所在文件夹”在 Onboarding 未完成时也可用。
- 每个失败状态只显示一个主要修复动作；“复制设置诊断”不包含用户名、路径、设备名、UUID、蓝牙地址、文字、音频或密钥。
- 完成页环境回退可分别定向返回权限、遥控器或音频页，不重新开始完整流程。

单元测试和构建不能替代系统权限、蓝牙、真实 iPhone Nearby、手机 Safari 网页版、真实音频设备、Sparkle 升级首次启动、豆包、微信输入法、Typeless、微信 App 快捷键及其他第三方工具验收。

## 验证分层

1. 流程层：状态机、持久化、门禁、重新运行和旧设置保留。
2. 页面层：实体遥控器检查生产视图浅色/深色各 9 张，iPhone/网页各检查浅色/深色 10 张截图，覆盖 `1020 × 772`、中英文、字号、裁切和视觉一致性。
3. 产物层：Release 构建、签名、公证、安装包、锁屏启动、资产摘要和 Sparkle 更新发现。
4. 现场层：真实权限历史、RC003、iPhone Nearby、手机 Safari 网页版、MiRemoteV 2ch、第三方语音工具的真实文字写入来源、前后台与断连恢复。

只有现场层可以证明用户机器上的完整链路可用；前面三层用于尽早发现确定性回归。
